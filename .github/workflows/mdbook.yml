name: Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
=======
<<<<<<< HEAD
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
    - uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
        key: mdbook-${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
    - name: Install mdbook
      run: |
        which mdbook || cargo install mdbook
        which mdbook-keeper || cargo install mdbook-keeper
        which mdbook-cmdrun || cargo install mdbook-cmdrun
        which mdbook-footnote || cargo install mdbook-footnote
    - name: Deploy GitHub Pages
      run: |
        # This assumes your book is in the root of your repository.
        # Just add a `cd` here if you need to change to another directory.
        cd book
        mdbook build
        git worktree add gh-pages
        git config user.name "Deploy from CI"
        git config user.email ""
        cd gh-pages
        # Delete the ref to avoid keeping history.
        git update-ref -d refs/heads/gh-pages
        rm -rf *
        mv ../book/* .
        git add .
        git commit -m "Deploy $GITHUB_SHA to gh-pages"
        git push --force --set-upstream origin gh-pages
=======
>>>>>>> 124b1ed (Doc: Updates on README.md that includes Details on Objectives ,Personal notes, Approach, and Notes)
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: mdbook-${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
      - name: Install mdbook
        run: |
          which mdbook || cargo install mdbook
          which mdbook-keeper || cargo install mdbook-keeper
          which mdbook-cmdrun || cargo install mdbook-cmdrun
          which mdbook-footnote || cargo install mdbook-footnote
      - name: Deploy GitHub Pages
        run: |
          # This assumes your book is in the root of your repository.
          # Just add a `cd` here if you need to change to another directory.
          cd book
          mdbook build
          git worktree add gh-pages
          git config user.name "Deploy from CI"
          git config user.email ""
          cd gh-pages
          # Delete the ref to avoid keeping history.
          git update-ref -d refs/heads/gh-pages
          rm -rf *
          mv ../book/* .
          git add .
          git commit -m "Deploy $GITHUB_SHA to gh-pages"
          git push --force --set-upstream origin gh-pages
<<<<<<< HEAD
=======
>>>>>>> b5a4c64 (Initial commit: Going through lifetimeKata)
>>>>>>> 124b1ed (Doc: Updates on README.md that includes Details on Objectives ,Personal notes, Approach, and Notes)
